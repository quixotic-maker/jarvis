# Phase 4.3 RAG系统开发计划

**阶段**: Phase 4.3  
**目标**: 实现检索增强生成(RAG)系统  
**预计时间**: 1-2周  
**状态**: 🚀 启动中

---

## 🎯 核心目标

### 业务目标
1. 让Jarvis能够基于用户自己的文档/知识库回答问题
2. 提供长期记忆能力（对话历史、用户偏好）
3. 支持项目级知识管理（代码库、文档）
4. 增强回答的准确性和相关性

### 技术目标
1. 构建完整的RAG Pipeline
2. 集成向量数据库（Chroma优先）
3. 实现高效的文档处理和检索
4. 提供知识库管理界面

---

## 📋 详细任务分解

### 第1周：基础设施搭建

#### Day 1-2: 向量数据库集成
**优先级**: 🔴 P0

**技术选型**:
- **Vector DB**: Chroma（易用、轻量、Python友好）
- **Embedding**: OpenAI text-embedding-ada-002（高质量）
- **Fallback**: 本地Sentence-Transformers（离线支持）

**任务**:
- [ ] Chroma环境搭建
  - [ ] 安装依赖: `pip install chromadb`
  - [ ] 初始化Collection
  - [ ] 配置持久化路径
- [ ] Embedding服务封装
  - [ ] OpenAI Embedding API
  - [ ] 本地模型Fallback（sentence-transformers）
  - [ ] 批量Embedding优化
- [ ] 基础CRUD操作
  - [ ] 文档插入（insert）
  - [ ] 相似度搜索（query）
  - [ ] 文档更新（update）
  - [ ] 文档删除（delete）

**验收标准**:
- ✅ 成功插入100条测试文档
- ✅ 查询响应时间 < 100ms
- ✅ 准确召回相关文档（Top-3准确率 > 80%）

---

#### Day 3-4: 文档处理Pipeline
**优先级**: 🔴 P0

**核心功能**:
1. **文档分块（Chunking）**
   - 策略: 固定长度 + 重叠窗口
   - Chunk大小: 500-1000字符
   - 重叠: 100-200字符（保持上下文连续性）

2. **Metadata提取**
   - 文档来源（source）
   - 创建时间（timestamp）
   - 文档类型（doc_type: txt, md, pdf, code）
   - 作者/所有者（author）
   - 标签（tags）

3. **多格式支持**
   - ✅ 纯文本（.txt）
   - ✅ Markdown（.md）
   - 🔄 PDF（.pdf）- 使用PyPDF2
   - 🔄 Word（.docx）- 使用python-docx
   - 🔄 代码文件（.py, .js, .ts等）

**任务**:
- [ ] 实现ChunkingService
  - [ ] RecursiveCharacterTextSplitter
  - [ ] 支持自定义分隔符
  - [ ] 保留代码结构（函数、类边界）
- [ ] 实现MetadataExtractor
  - [ ] 基础Metadata提取
  - [ ] 文件类型检测
  - [ ] 编码自动识别
- [ ] 文档加载器（DocumentLoader）
  - [ ] TextLoader
  - [ ] MarkdownLoader
  - [ ] PDFLoader（可选）
  - [ ] CodeLoader（保留语法结构）

**验收标准**:
- ✅ 正确分块长文档（10000+字符）
- ✅ Metadata完整性100%
- ✅ 支持至少3种文档格式

---

#### Day 5: 检索服务实现
**优先级**: 🔴 P0

**核心功能**:
1. **语义搜索**
   - 基于Embedding的相似度检索
   - Top-K结果返回（默认K=3-5）
   - 相似度阈值过滤（threshold > 0.7）

2. **Hybrid Search**（可选）
   - 语义搜索 + 关键词搜索
   - BM25算法集成
   - 结果融合（Reciprocal Rank Fusion）

3. **上下文注入**
   - 检索结果格式化
   - 注入到LLM Prompt
   - 来源引用（Citations）

**任务**:
- [ ] 实现RetrievalService
  - [ ] semantic_search(): 语义检索
  - [ ] hybrid_search(): 混合检索（可选）
  - [ ] format_context(): 上下文格式化
- [ ] 集成到Agent
  - [ ] 修改InfoRetrievalAgent
  - [ ] 添加RAG模式开关
  - [ ] 结果来源标注

**验收标准**:
- ✅ 检索相关性 > 80%
- ✅ 端到端检索延迟 < 200ms
- ✅ 正确注入上下文到Prompt

---

### 第2周：高级功能和集成

#### Day 6-7: 知识库管理
**优先级**: 🟡 P1

**功能设计**:
1. **知识库隔离**
   - 用户级知识库（user_kb）
   - 项目级知识库（project_kb）
   - 全局知识库（global_kb）

2. **知识库操作**
   - 创建知识库
   - 上传文档
   - 删除文档
   - 查看知识库统计

3. **API端点**
   ```
   POST   /api/knowledge-base/create
   POST   /api/knowledge-base/upload
   GET    /api/knowledge-base/list
   DELETE /api/knowledge-base/documents/:id
   GET    /api/knowledge-base/stats
   ```

**任务**:
- [ ] 数据库Schema设计
  - [ ] knowledge_bases表
  - [ ] documents表
  - [ ] document_chunks表（关联Chroma ID）
- [ ] KnowledgeBaseService
  - [ ] 知识库CRUD
  - [ ] 文档管理
  - [ ] 权限控制
- [ ] REST API实现
  - [ ] 路由定义
  - [ ] 请求验证
  - [ ] 错误处理

**验收标准**:
- ✅ 支持多知识库隔离
- ✅ 文档上传成功率100%
- ✅ API响应时间 < 500ms

---

#### Day 8-9: 前端集成
**优先级**: 🟡 P1

**功能设计**:
1. **知识库管理页面**（BrainPage扩展）
   - 知识库列表
   - 文档上传界面
   - 文档列表展示
   - 删除/编辑操作

2. **Chat增强**
   - RAG模式开关
   - 来源引用显示
   - 相关文档推荐

**任务**:
- [ ] 前端组件开发
  - [ ] KnowledgeBaseList
  - [ ] DocumentUploader
  - [ ] DocumentViewer
  - [ ] CitationDisplay（来源引用）
- [ ] API集成
  - [ ] 调用知识库API
  - [ ] 文件上传处理
  - [ ] 实时状态更新

**验收标准**:
- ✅ 可视化管理界面完整
- ✅ 文件上传支持拖拽
- ✅ 来源引用正确显示

---

#### Day 10: 优化和测试
**优先级**: 🟢 P2

**优化方向**:
1. **性能优化**
   - Embedding批量生成
   - 向量检索缓存
   - 异步处理大文件

2. **质量优化**
   - Re-ranking（重排序）
   - Query扩展
   - 负样本过滤

3. **用户体验**
   - 上传进度条
   - 处理状态提示
   - 错误友好提示

**任务**:
- [ ] 性能基准测试
  - [ ] 检索延迟测试
  - [ ] 并发压力测试
  - [ ] 内存占用分析
- [ ] 端到端测试
  - [ ] 文档上传 → 检索 → 回答流程
  - [ ] 多知识库场景
  - [ ] 边界情况测试

**验收标准**:
- ✅ 检索P99延迟 < 300ms
- ✅ 支持并发10+用户
- ✅ 端到端成功率 > 95%

---

## 🏗️ 技术架构

### 系统架构图
```
┌─────────────────────────────────────────────────────┐
│                   Frontend (React)                   │
│  ┌──────────────┐  ┌──────────────┐  ┌───────────┐ │
│  │ BrainPage    │  │ ChatInterface│  │ Upload UI │ │
│  └──────────────┘  └──────────────┘  └───────────┘ │
└─────────────────────────────────────────────────────┘
                          ↓ HTTP API
┌─────────────────────────────────────────────────────┐
│              Backend (FastAPI)                       │
│  ┌──────────────────────────────────────────────┐  │
│  │         RAG Service Layer                     │  │
│  │  ┌────────────┐  ┌──────────┐  ┌──────────┐ │  │
│  │  │ Retrieval  │  │ Document │  │ Knowledge│ │  │
│  │  │ Service    │  │ Processor│  │ Base Mgr │ │  │
│  │  └────────────┘  └──────────┘  └──────────┘ │  │
│  └──────────────────────────────────────────────┘  │
│                          ↓                           │
│  ┌──────────────────────────────────────────────┐  │
│  │         Agent Integration                     │  │
│  │  InfoRetrievalAgent (RAG-enabled)            │  │
│  └──────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────┘
                          ↓
┌─────────────────────────────────────────────────────┐
│              Data Layer                              │
│  ┌──────────┐  ┌──────────┐  ┌──────────────────┐ │
│  │ Chroma   │  │ Postgres │  │ File Storage     │ │
│  │ (Vector) │  │ (Metadata)│ │ (原始文档)       │ │
│  └──────────┘  └──────────┘  └──────────────────┘ │
└─────────────────────────────────────────────────────┘
```

### 核心模块

**1. EmbeddingService** (`app/rag/embedding_service.py`)
```python
class EmbeddingService:
    async def embed_text(text: str) -> List[float]
    async def embed_batch(texts: List[str]) -> List[List[float]]
    def get_model_info() -> Dict
```

**2. VectorStoreService** (`app/rag/vector_store.py`)
```python
class VectorStoreService:
    async def add_documents(documents: List[Document])
    async def search(query: str, k: int = 5) -> List[Document]
    async def delete(ids: List[str])
    async def get_stats() -> Dict
```

**3. DocumentProcessor** (`app/rag/document_processor.py`)
```python
class DocumentProcessor:
    def load_document(file_path: str) -> Document
    def chunk_document(doc: Document) -> List[Chunk]
    def extract_metadata(doc: Document) -> Dict
```

**4. RetrievalService** (`app/rag/retrieval_service.py`)
```python
class RetrievalService:
    async def retrieve(query: str, kb_id: str, top_k: int = 5)
    async def retrieve_and_format(query: str, kb_id: str)
```

**5. KnowledgeBaseService** (`app/rag/knowledge_base_service.py`)
```python
class KnowledgeBaseService:
    async def create_kb(name: str, owner_id: str)
    async def upload_document(kb_id: str, file)
    async def list_documents(kb_id: str)
    async def delete_document(doc_id: str)
```

---

## 📦 技术栈

### 核心依赖
```python
chromadb==0.4.22           # 向量数据库
sentence-transformers      # 本地Embedding（可选）
openai                     # OpenAI Embedding API
langchain                  # RAG工具链（可选，按需引入）
pypdf2                     # PDF处理（可选）
python-docx                # Word处理（可选）
```

### 可选增强
```python
rank-bm25                  # BM25关键词检索
faiss-cpu                  # 备选向量库
tiktoken                   # Token计数
```

---

## 🎯 里程碑

### Milestone 1: 核心RAG功能（Day 1-5）
- ✅ 向量数据库可用
- ✅ 文档处理Pipeline
- ✅ 基础检索功能

### Milestone 2: 知识库管理（Day 6-7）
- ✅ 多知识库支持
- ✅ API完整实现

### Milestone 3: 前端集成（Day 8-9）
- ✅ 管理界面可用
- ✅ Chat集成RAG

### Milestone 4: 优化上线（Day 10）
- ✅ 性能达标
- ✅ 测试通过

---

## 🚀 启动第一步

**立即开始**: Day 1 - 向量数据库集成

下一个命令将创建基础项目结构：
```bash
# 创建RAG模块目录
mkdir -p backend/app/rag
touch backend/app/rag/__init__.py
touch backend/app/rag/embedding_service.py
touch backend/app/rag/vector_store.py
touch backend/app/rag/document_processor.py
touch backend/app/rag/retrieval_service.py
touch backend/app/rag/knowledge_base_service.py

# 安装依赖
pip install chromadb sentence-transformers
```

准备好开始了吗？ 🚀
